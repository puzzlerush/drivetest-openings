{
  "name": "drivetest-openings",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "backend": "cd backend && npm run devstart",
    "dev": "concurrently --kill-others-on-fail \"npm run backend\" \"npm run client\""
  },
  "dependencies": {
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
